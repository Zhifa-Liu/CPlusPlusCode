#include <iostream>
#include <math.h>
using namespace std;

double exponent(double);//????????
int num=0;//?????? ????????? ?????§Ü??????

void emain()
{
	double x;
	do {
		cout << "Please input the value of x: ";
		cin >> x;
		cout << "the exponent value of " << x << " is " << exponent(x) << endl;
		system("pause");
	} while (x != 0);
}

double exponent(double x)
{
	num++;

	static int n = 0;//????¦Ì????????§Ö??????? ?????????????????????

	double fi = 1;
	double fx = x;
	double sum = 1;

	for (int i = 1;i < 10;i++)
	{
		sum += fx / fi;
		fi *= i + 1;
		fx = fx * x;
	}

	return sum;
}//????????